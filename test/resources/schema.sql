 create database saga;
 use saga;
 
 
CREATE TABLE CUSTOMER(
   ID   INT              NOT NULL,
   NAME VARCHAR (20)     NOT NULL,
   AGE  INT              NOT NULL,
   ADDRESS  CHAR (25) 
);